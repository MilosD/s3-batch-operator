---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: s3batchjobs.batch.batch.operator
spec:
  group: batch.batch.operator
  names:
    kind: S3BatchJob
    listKind: S3BatchJobList
    plural: s3batchjobs
    singular: s3batchjob
  scope: Namespaced
  versions:
  - name: v1alphav1
    schema:
      openAPIV3Schema:
        description: S3BatchJob is the Schema for the s3batchjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: S3BatchJobSpec defines the desired state of S3BatchJob.
            properties:
              bucket:
                description: Foo is an example field of S3BatchJob. Edit s3batchjob_types.go
                  to remove/update
                type: string
              jobName:
                type: string
              manifest:
                type: string
              roleArn:
                type: string
            required:
            - bucket
            - jobName
            - manifest
            - roleArn
            type: object
          status:
            description: S3BatchJobStatus defines the observed state of S3BatchJob.
            properties:
              jobid:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              message:
                type: string
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
